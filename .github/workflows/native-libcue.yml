name: "Fuzz native libcue"

on:
  workflow_dispatch:
    inputs:
      fuzz_duration:
        description: "Fuzzing duration in seconds"
        default: "1800"
  schedule:
    - cron: "0 13 * * 0"

env:
  PROJECT: libcue
  FUZZ_DURATION: ${{ inputs.fuzz_duration || '1800' }}
  CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
  CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
  R2_BUCKET: osv-fuzz-seeds
  AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
  AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}

jobs:
  fuzz:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Checkout osv-fuzz
        uses: actions/checkout@v4

      - name: Download native seeds from R2
        run: bash scripts/download_native_seeds.sh "$PROJECT"

      - name: Validate seeds
        run: |
          seed_count=$(find "seeds/$PROJECT/" -name "*.bin" -type f 2>/dev/null | wc -l)
          echo "[+] Seeds available: $seed_count"
          ls -la "seeds/$PROJECT/" 2>/dev/null | head -20 || true

      - name: Install AFL++ and dependencies
        run: bash scripts/setup_aflpp.sh

      - name: Install libcue build dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq bison flex zlib1g-dev clang cmake git 2>/dev/null

      - name: Clone latest libcue source
        run: |
          mkdir -p /tmp/src
          git clone --depth 1 https://github.com/lipnitsk/libcue.git /tmp/src/libcue

      - name: Build libcue harness with AFL++ + ASan
        run: |
          set -e
          export AFL_USE_ASAN=1
          cd /tmp/src/libcue
          mkdir -p build && cd build
          LIB_FUZZING_ENGINE="" \
          cmake -DBUILD_FUZZER=ON \
                -DCMAKE_BUILD_TYPE=Debug \
                -DCMAKE_C_COMPILER=afl-cc \
                -DCMAKE_CXX_COMPILER=afl-c++ \
                -DCMAKE_C_FLAGS="-g -O1 -fsanitize=address -fno-omit-frame-pointer" \
                -DCMAKE_CXX_FLAGS="-g -O1 -fsanitize=address -fno-omit-frame-pointer" \
                -DCMAKE_EXE_LINKER_FLAGS="-fsanitize=address" \
                ..
          make -j"$(nproc)" fuzz
          # Record binary location for later steps
          BIN="/tmp/src/libcue/build/fuzz"
          if [ ! -x "$BIN" ]; then
            echo "[!] Expected harness not found at $BIN"
            find /tmp/src/libcue/build -maxdepth 2 -type f -executable | head
            exit 1
          fi
          echo "BINDIR=/tmp/src/libcue/build" >> "$GITHUB_ENV"
          echo "FUZZ_BIN=$BIN" >> "$GITHUB_ENV"
          echo "[+] Built harness: $BIN"
          ls -la "$BIN"

      - name: Dry-run replay (lost-bug check)
        run: |
          set +e
          mkdir -p results/dry_run
          bname=$(basename "$FUZZ_BIN")
          crash_count=0
          total=0
          for seed in "seeds/$PROJECT"/*.bin; do
            [ -f "$seed" ] || continue
            sname=$(basename "$seed" .bin)
            total=$((total + 1))
            echo "[*] Replaying $sname through $bname..."
            ASAN_OPTIONS="abort_on_error=1:detect_leaks=0" \
              timeout 60 "$FUZZ_BIN" "$seed" > /dev/null 2> results/dry_run/.stderr_tmp
            exit_code=$?
            if [ "$exit_code" -ne 0 ] && [ "$exit_code" -ne 124 ]; then
              echo "  [!] CRASH: $sname (exit=$exit_code)"
              mv results/dry_run/.stderr_tmp "results/dry_run/${bname}_${sname}.txt"
              cp "$seed" "results/dry_run/${bname}_${sname}.bin"
              crash_count=$((crash_count + 1))
            else
              rm -f results/dry_run/.stderr_tmp
            fi
          done
          echo "[+] Dry run complete: $crash_count crashes out of $total tests"

      - name: Fuzz with AFL++
        run: |
          set +e
          mkdir -p results/afl
          export AFL_SKIP_CPUFREQ=1
          export AFL_I_DONT_CARE_ABOUT_MISSING_CRASHES=1
          afl-fuzz -i "seeds/$PROJECT" -o results/afl -V "$FUZZ_DURATION" \
            -- "$FUZZ_BIN" @@
          echo "[+] Fuzzing finished (exit=$?)"
          exit 0
        continue-on-error: true

      - name: Triage AFL++ crashes
        run: |
          set +e
          mkdir -p results/fuzz
          bname=$(basename "$FUZZ_BIN")
          crash_dir="results/afl/default/crashes"
          if [ ! -d "$crash_dir" ]; then
            echo "[+] No crash directory found: $crash_dir"
            exit 0
          fi
          for crash in "$crash_dir"/*; do
            [ -f "$crash" ] || continue
            cname=$(basename "$crash")
            [ "$cname" = "README.txt" ] && continue
            echo "[*] Triaging $cname..."
            ASAN_OPTIONS="abort_on_error=1:detect_leaks=0" \
              timeout 60 "$FUZZ_BIN" "$crash" > /dev/null 2> "results/fuzz/${bname}_${cname}.txt"
            cp "$crash" "results/fuzz/${bname}_${cname}.bin"
          done
          echo "[+] Triage complete"

      - name: Generate report
        run: python3 scripts/report.py results/ "$PROJECT"

